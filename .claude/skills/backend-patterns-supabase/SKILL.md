---
name: backend-patterns-supabase
description: Supabase API patterns for CapeTown GIS
---
API route: Session validation → Tenant context → Query (RLS filters) → Return
Three-tier fallback: LIVE → CACHED → MOCK
