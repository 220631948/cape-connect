# Layer Analysis: Spatial Data & PostGIS
> Generated by: WU-7 (Sequential Fallback) | Date: 2026-03-14

## Summary
- Migrations create comprehensive spatial schema with GiST indexes and composite indexes for RLS performance.
- ST_IsValid geometry constraints are missing from migrations — invalid geometries may be inserted.
- Local development uses PostGIS on PostgreSQL 17 (kartoza/postgis:17-3.5) while hosted Supabase runs PostgreSQL 15 — version mismatch risk.
- Martin tile server is unpinned (`ghcr.io/maplibre/martin:latest`) in docker-compose.yml which risks breaking tile stack reproducibility.
- Map init enforces Western Cape bounds and MapLibre/Cesium integrations follow best practices (Map removal on unmount, Cesium dynamic import).

## Findings
- Missing ST_IsValid constraints: grep for `ST_IsValid` in supabase/migrations/ returned no matches — recommended in analysis_04_backend.md.
- Postgres version mismatch: docker-compose.yml uses kartoza/postgis:17-3.5 (analysis_04_backend.md lines 44–46).
- Martin unpinned: docker-compose.yml line 34 shows `ghcr.io/maplibre/martin:latest` (analysis_05_devops.md).
- Client-side spatial code: src/components/map/MapContainer.tsx handles bounds and cleanup correctly (analysis_01_frontend.md lines 16–18).

## Recommendations
P0 — Must fix before M17 ships
1. Add geometry validity constraints in migrations:
   - Example migration snippet: `ALTER TABLE properties ADD CONSTRAINT properties_valid_geom CHECK (ST_IsValid(geometry));`
   - Files: supabase/migrations/* (create a small migration file to add constraints)
   - Effort: S | Risk: LOW

P1 — Should fix this sprint
2. Align local PostGIS version to Supabase (use kartoza/postgis:15-3.4) or ensure migrations avoid 17-only features.
   - Files: docker-compose.yml
   - Effort: S | Risk: LOW

3. Pin Martin image to a tested release (e.g., `ghcr.io/maplibre/martin:v0.15.0` or stable tag). Update docker-compose.yml image reference.
   - Effort: S | Risk: LOW

P2 — Nice to have
4. Add spatial data validation tests: input fuzzing for geometry fields, unit tests that assert ST_IsValid on inserts.
5. Add migration-time checks in CI: spin up PostGIS container with the target Postgres version and run all migrations (see CI recommendations in analysis_08_testing.md).

## Concrete steps
- Create migration: supabase/migrations/20260314_add_valid_geom_constraints.sql with ALTER TABLE statements for properties, zones, parcels as needed.
- Update docker-compose.yml: replace kartoza/postgis:17-3.5 with kartoza/postgis:15-3.4 or otherwise match hosted version.
- Update docker-compose.yml: pin martin image to specific version.
- Add a Vitest integration: tests/spatial/validate_geom.test.ts that runs sample inserts and asserts ST_IsValid(geom).

## Acceptance criteria
- New migration file exists and runs cleanly against a local PostGIS instance
- CI job runs migrations against the selected Postgres version without errors
- Martin image uses a pinned tag in docker-compose.yml and is documented in CHANGELOG

## Rollback / Mitigation
- If adding ST_IsValid constraints fails due to existing invalid rows, add a data-cleaning migration first to fix or drop invalid geometries into a quarantine table for manual review.

Sources / Evidence
- supabase/migrations/ (initial schema & index choices) — referenced in analysis_04_backend.md
- docker-compose.yml lines 6, 22, 34 — PostGIS image, port mapping, Martin image (analysis_05_devops.md)
- src/components/map/MapContainer.tsx — Map init/cleanup (analysis_01_frontend.md)
