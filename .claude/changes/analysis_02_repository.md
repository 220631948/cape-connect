# Layer Analysis: Repository Structure & Worktree Topology
> Generated by: WU-2 (Coordinator Fallback) | Date: 2026-03-14

## Summary
- 8 full worktrees (each with 99 source files, 12 migrations, 9 CI workflows) and 7 skeleton worktrees under `.claude/worktrees/`
- Root repository has only 5 source files (4 API routes + 1 page) — no package.json, no tsconfig.json, no next.config.ts
- CI/CD workflows exist only in worktrees — root `.github/workflows/` is missing
- All 8 full worktrees appear identical (same file count) — canonical source is ambiguous
- `git clone` of the root repo yields a broken, non-functional project

## Findings

### Finding 2.1: Root Repository is Incomplete — Cannot Build
- **Severity:** CRITICAL
- **Category:** Architecture
- **Files Affected:** Root `src/`, `package.json` (missing), `tsconfig.json` (missing), `next.config.ts` (missing)
- **Description:** The root repository contains only 5 source files in `src/app/` (4 API routes + invite page). The full application with 99 source files, all configurations, and all migrations lives exclusively in `.claude/worktrees/agent-*/` directories. Running `npm install` or `npm run build` at root is impossible.
- **Evidence:** `find src/ -name "*.tsx" | wc -l` → 1 at root; `find .claude/worktrees/agent-a3419dcd/src/ -name "*.tsx" -o -name "*.ts" | wc -l` → 99
- **Recommendation:** Consolidate the canonical worktree (agent-a3419dcd) to root: copy src/, package.json, tsconfig.json, next.config.ts, vitest.config.ts, Dockerfile, .env.example, supabase/migrations/
- **Effort:** L | **Risk:** HIGH
- **Acceptance Criteria:** `npm install && npm run build && npm test` passes at root
- **Rollback:** Git revert the consolidation commit

### Finding 2.2: CI/CD Workflows Not at Root
- **Severity:** CRITICAL
- **Category:** Architecture
- **Files Affected:** `.github/workflows/` (missing at root)
- **Description:** 9 GitHub Actions workflows exist only inside worktrees. GitHub only executes workflows from the repo root `.github/workflows/`. This means no CI runs on any push or PR.
- **Evidence:** `ls .github/workflows/` at root → missing or empty; `ls .claude/worktrees/agent-a3419dcd/.github/workflows/` → 9 yml files
- **Recommendation:** Copy `.github/workflows/` from canonical worktree to root
- **Effort:** S | **Risk:** LOW
- **Acceptance Criteria:** Push to main triggers CI pipeline on GitHub

### Finding 2.3: Multiple Identical Full Worktrees — Storage Waste
- **Severity:** LOW
- **Category:** Maintainability
- **Files Affected:** `.claude/worktrees/agent-{a3419dcd,a3a7802d,a43581f3,a440225f,a5f622ef,a7f8f6e7,a963c860,abe7be39}/`
- **Description:** 8 full worktrees each contain the complete app (~99 files, ~12 migrations). These appear to be parallel agent workspaces that were never cleaned up.
- **Evidence:** `for d in .claude/worktrees/agent-*/; do count=$(find "$d/src" -name "*.tsx" -o -name "*.ts" 2>/dev/null | wc -l); echo "$count $d"; done` → all show 99
- **Recommendation:** After consolidation to root, remove worktrees with `git worktree remove` or delete the directories
- **Effort:** S | **Risk:** LOW

### Finding 2.4: 7 Skeleton Worktrees — Empty Shells
- **Severity:** LOW
- **Category:** Maintainability
- **Files Affected:** `.claude/worktrees/agent-{a22f449f,a347d937,a409530f,a4a9c7b9,ac4b51c3,ae35108b,aeefa3ea}/`
- **Description:** 7 worktrees contain minimal files (no src/), likely failed or partial agent runs.
- **Recommendation:** Delete skeleton worktrees
- **Effort:** S | **Risk:** LOW

### Finding 2.5: No Root .env.example
- **Severity:** MEDIUM
- **Category:** Maintainability
- **Files Affected:** `.env.example` (missing at root)
- **Description:** `.env.example` exists only in worktrees. New developers cloning the repo won't know what environment variables are needed.
- **Evidence:** Worktree has 29 env vars documented in `.env.example`
- **Recommendation:** Copy from worktree to root
- **Effort:** S | **Risk:** LOW

### Finding 2.6: No Root CODEOWNERS
- **Severity:** LOW
- **Category:** Maintainability
- **Files Affected:** `.github/CODEOWNERS` (missing)
- **Description:** No code ownership enforcement. PRs aren't auto-assigned to reviewers.
- **Recommendation:** Create CODEOWNERS mapping directories to team members
- **Effort:** S | **Risk:** LOW

## Recommendations (Prioritized)
### P0 — Must Fix Before M17 Ships
1. Consolidate canonical worktree code to repo root (Finding 2.1)
2. Move CI workflows to root `.github/workflows/` (Finding 2.2)

### P1 — Should Fix This Sprint
3. Copy `.env.example` to root (Finding 2.5)
4. Add `.github/CODEOWNERS` (Finding 2.6)

### P2 — Nice to Have
5. Clean up 8 full worktrees after consolidation (Finding 2.3)
6. Delete 7 skeleton worktrees (Finding 2.4)
