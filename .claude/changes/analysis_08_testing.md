# Layer Analysis: Testing & CI
> Generated by: WU-8 (Sequential Fallback) | Date: 2026-03-14

## Summary
- Project includes unit test tooling (Vitest) but the test surface is small and there is no visible end-to-end/integration suite.
- CI workflows (9 files) exist only in `.claude/worktrees/*/.github/workflows/` and not at repo root — CI does not run on pushes (critical, analysis_02_repository.md).
- CI `ci.yml` has Supabase migration verification commented out and `|| true` masking RLS/CRS failures (analysis_05_devops.md lines 59–66).
- Seed data inserts into `auth.users` in a migration which isn't portable to fresh Supabase instances (analysis_04_backend.md lines 66–73).

## Findings
- Unit tests present (Vitest) but coverage is low and key contract tests (API contracts, RLS enforcement) are missing.
- No Playwright/Playwright-like e2e flows were found; dynamic UI elements (Map/Cesium) are not covered by automated UI tests.
- CI workflows not at root: `.github/workflows/` missing at repo root (analysis_02_repository.md lines 24–31).

## Recommendations
P0 — Must fix before M17 ships
1. Move CI workflows to repo root `.github/workflows/` so GitHub Actions run on PRs and pushes. (analysis_02_repository.md)
   - Effort: S | Risk: LOW

2. Re-enable Supabase migration verification in CI: run migrations against a disposable Postgres container (use the same Postgres version as Supabase hosted) and remove `|| true` masking so RLS/CRS violations fail the build.
   - Files: .github/workflows/ci.yml
   - Effort: M | Risk: MED

P1 — Should fix this sprint
3. Add contract tests for API endpoints using OpenAPI or Pact, and add unit tests for RLS-sensitive flows.
   - Tools: pact-js or supertest + vitest
   - Effort: M | Risk: LOW

4. Add Playwright e2e suite for key user flows: login, map load, 3D mode toggle, basic admin actions.
   - Effort: L | Risk: MED

P2 — Nice to have
5. Add CI matrix job that runs migrations against both Postgres 15 and 17 (if supporting both), and include a migration-lint step.
6. Move seed user creation out of a migration to a separate script that uses Supabase admin API (protect with service role key).

## Concrete commands & CI snippets
- CI job to run migrations:
  - docker-compose -f .github/ci/docker-compose.migration.yml up -d db
  - supabase migrations apply --project-ref <local> (or psql -f migrations/*.sql)
- Example CI check to fail on RLS/CRS violations: remove `|| true` and assert exit code != 0 when violation detected.

## Acceptance criteria
- PR triggers run CI on GitHub
- CI runs migration job and fails on RLS/CRS violations
- Basic contract tests (1-2 endpoints) exist and pass locally

## Rollback / Mitigation
- If re-enabling migration checks causes many failures, gate the change behind a temporary `ci/migrations` feature flag and run nightly job while fixing failures incrementally.

Sources / Evidence
- .github/workflows files located in .claude/worktrees/ (analysis_02_repository.md)
- ci.yml comments and `|| true` usage (analysis_05_devops.md lines 59–66)
- supabase/migrations/seed_data.sql insertion into auth.users (analysis_04_backend.md)
