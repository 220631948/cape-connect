# Project Analysis Changelog — 2026-03-14

> Generated by: Project Analyst Agent (Claude Code Opus 4.6)
> Date: 2026-03-14T16:30:00Z
> Scope: Full repository strategic research and audit

---

## Methodology

1. Read and summarized `docs/research/PROJECT_ANALYSIS.md` (10 bullets)
2. Performed repo-wide scan across 5 layers: Frontend, Repository, Services/API, Backend/DB, DevOps
3. Read 99 source files inventory, 12 SQL migrations, 9 CI workflows, 5 config files
4. Cross-referenced findings against CLAUDE.md non-negotiable rules (10 rules)
5. Produced prioritized recommendations with effort/risk ratings

---

## Findings Summary (23 Total)

### CRITICAL (2)
| ID | Finding | Evidence | Recommendation |
|----|---------|----------|----------------|
| F1 | Full app code lives only in `.claude/worktrees/agent-*/` — root `src/` has only 5 files, no root `package.json` | `find src/ -name "*.tsx" \| wc -l` → 1 at root vs 99 in worktrees | Consolidate canonical worktree to root |
| F5 | `.github/workflows/` with 9 CI pipelines exists only in worktrees | Root has no `.github/workflows/` | Copy workflows to root |

### HIGH (3)
| ID | Finding | Evidence | Recommendation |
|----|---------|----------|----------------|
| F2 | `@supabase/auth-helpers-nextjs@^0.15.0` is deprecated | `package.json` line 18; Supabase docs recommend `@supabase/ssr` | Migrate to `@supabase/ssr` |
| F3 | Zero input validation on 22 API routes | All routes use raw `request.json()` without zod/AJV | Add zod schemas |
| F10 | 11 test files for 87 source files (~12.6% coverage) | `find src/ -name "*.test.*" \| wc -l` → 11 | Increase to 40%+ |

### MEDIUM (6)
| ID | Finding | Evidence | Recommendation |
|----|---------|----------|----------------|
| F4 | Server-side code uses `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `src/lib/supabase/server.ts` line 16 | Use `SUPABASE_SERVICE_ROLE_KEY` for admin ops |
| F6 | Martin Docker image not pinned | `docker-compose.yml` line 34: `ghcr.io/maplibre/martin:latest` | Pin to specific version |
| F8 | Cookie security flags not explicit | `server.ts` cookie handlers pass options without secure/httpOnly | Add explicit flags |
| F9 | Very loose npm version ranges | `package.json`: `"next": "^15.0.0"` | Pin to specific minor |
| F11 | Error messages leaked to client | API routes return `error.message` | Sanitize to generic messages |
| POPIA | 9 DPA flags marked "ASSUMPTION — UNVERIFIED" | `docs/research/PROJECT_ANALYSIS.md` | Legal review required |

### LOW (3)
| ID | Finding | Evidence | Recommendation |
|----|---------|----------|----------------|
| F7 | PostGIS 17 (Docker) vs PostgreSQL 15 (Supabase) | `docker-compose.yml`: `kartoza/postgis:17-3.5`; CLAUDE.md says PostgreSQL 15 | Monitor compatibility |
| F12 | Dockerfile copies full node_modules | `Dockerfile` line 32 | Prune devDependencies |
| CSP | CSP allows `unsafe-eval` and `unsafe-inline` | `next.config.ts` lines 42-44 | Tighten when possible |

---

## Recommendations Mapped to Repo Evidence

### P0 — Must Fix Before M17 Ships

1. **Consolidate worktree code to root**
   - Evidence: Root `src/app/api/` has 4 files; `.claude/worktrees/agent-a3419dcd/src/app/api/` has 22 routes
   - Files: Copy from worktree: `src/`, `package.json`, `tsconfig.json`, `next.config.ts`, `vitest.config.ts`, `.env.example`, `Dockerfile`, `supabase/migrations/`, `.github/workflows/`
   - Commands: `cp -r .claude/worktrees/agent-a3419dcd/{src,package.json,tsconfig.json,next.config.ts,vitest.config.ts,.env.example,Dockerfile} .`
   - Acceptance: `npm install && npm run build && npm test` passes at root

2. **Migrate `@supabase/auth-helpers-nextjs` → `@supabase/ssr`**
   - Evidence: `package.json` line 18: `"@supabase/auth-helpers-nextjs": "^0.15.0"`
   - Files: `src/lib/supabase/server.ts`, `src/middleware.ts`, `src/lib/supabase/client.ts`
   - Commands: `npm uninstall @supabase/auth-helpers-nextjs && npm install @supabase/ssr`
   - Changes: Replace `createServerClient` import from `@supabase/auth-helpers-nextjs` to `@supabase/ssr`
   - Acceptance: Auth flow works end-to-end (login, session refresh, logout)
   - Rollback: Revert package.json and source imports

3. **Add zod input validation**
   - Evidence: All 22 API routes use raw `await request.json()` without validation
   - Commands: `npm install zod`
   - Files: All `src/app/api/**/route.ts` files
   - Acceptance: Invalid API payloads return 400 with descriptive errors

4. **Move CI workflows to root**
   - Evidence: `.github/workflows/` exists only in `.claude/worktrees/agent-a3419dcd/.github/workflows/`
   - Commands: `cp -r .claude/worktrees/agent-a3419dcd/.github .`
   - Files: 9 workflow YAML files
   - Acceptance: `git push` triggers CI pipeline on GitHub

5. **Pin Martin image version**
   - Evidence: `docker-compose.yml` line 34: `image: ghcr.io/maplibre/martin:latest`
   - Change: Pin to `ghcr.io/maplibre/martin:v0.15.0` (or latest stable)
   - Acceptance: `docker compose up martin` starts successfully with pinned version

6. **Audit SECURITY DEFINER RPCs**
   - Evidence: `analyze_area()` and `search_properties()` use SECURITY DEFINER
   - Risk: SECURITY DEFINER runs with function owner's privileges, bypassing RLS
   - Check: Verify these functions enforce tenant isolation internally via `current_setting('app.current_tenant')`
   - Status: Both functions DO check `app.current_tenant` ✅ — verified in migrations

### P1 — Should Fix This Sprint

7. **Pin npm dependencies** — Change `^15.0.0` → `~15.1.0` patterns
8. **Add React error boundaries** — Wrap MapContainer and CesiumViewer
9. **CSRF protection** — Add CSRF tokens to POST/PUT/DELETE routes
10. **ST_IsValid constraints** — Add `CHECK (ST_IsValid(geom))` to geometry columns
11. **Restrict PostGIS port** — Change `"5432:5432"` to `"127.0.0.1:5432:5432"`
12. **Remove Docker socket mount** — Find alternative for LocalStack
13. **Add CODEOWNERS** — Create `.github/CODEOWNERS` with team assignments
14. **Root .env.example** — Copy from worktree
15. **Increase test coverage** — Target auth, tenant isolation, spatial queries
16. **Cookie hardening** — Add explicit `secure: true, httpOnly: true, sameSite: 'lax'`
17. **Sanitize error messages** — Return generic errors, log details server-side

---

## SECRETS_REQUIRED (Masked)

| Variable | Purpose | Where Used |
|----------|---------|------------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase API endpoint | `server.ts`, `client.ts`, `middleware.ts` |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Public Supabase key | `server.ts`, `client.ts`, `middleware.ts` |
| `SUPABASE_SERVICE_ROLE_KEY` | Admin Supabase key | `docker-compose.yml` web service |
| `DATABASE_URL` | Direct PostgreSQL connection | `docker-compose.yml` |
| `POSTGRES_PASSWORD` | Database password | `docker-compose.yml` |
| `LOCALSTACK_AUTH_TOKEN` | LocalStack Pro license | `docker-compose.yml` |
| `OPENSKY_USERNAME` | OpenSky API auth | `src/lib/opensky-api.ts` |
| `OPENSKY_PASSWORD` | OpenSky API auth | `src/lib/opensky-api.ts` |
| `CESIUM_ION_TOKEN` | CesiumJS tile service | `src/components/map/CesiumViewer.tsx` |

---

## Commit Metadata

- **Branch name:** `chore/project-analysis-2026-03-14`
- **Commit message:** `docs: add strategic research audit with 23 findings and hardening roadmap`
- **Files changed:**
  - `PLAN.md` (new — full milestone plan + hardening roadmap)
  - `.claude/changes/project_analysis__20260314_163000.md` (new — this changelog)

---

## PR Body Text

```markdown
## Summary
- Comprehensive project audit covering 5 layers: Frontend, Repository, Services/API, Backend/DB, DevOps
- Identified 23 findings (2 CRITICAL, 3 HIGH, 6 MEDIUM, 3 LOW)
- Created root `PLAN.md` with complete milestone history + hardening roadmap
- Produced prioritized implementation plan: Track A (P0, 2-3 weeks) and Track B (P1+P2, 4-6 weeks)

## Key Findings
- **CRITICAL:** Full app code exists only in worktrees; root repo is incomplete
- **CRITICAL:** CI/CD workflows not at root level
- **HIGH:** `@supabase/auth-helpers-nextjs` is deprecated → migrate to `@supabase/ssr`
- **HIGH:** Zero input validation on 22 API routes
- **MEDIUM:** Server-side code uses anon key instead of service role key

## Test plan
- [ ] Verify `PLAN.md` renders correctly in GitHub markdown
- [ ] Verify changelog file is complete and accurate
- [ ] Review findings against CLAUDE.md rules
- [ ] Confirm no secrets are exposed in any file

Generated with [Claude Code](https://claude.com/claude-code)
```

---

## Validation Commands

```bash
# Verify PLAN.md exists and is well-formed
cat PLAN.md | head -5

# Verify changelog exists
ls -la .claude/changes/project_analysis__20260314_163000.md

# Verify no secrets in committed files
grep -rn "sk-\|Bearer \|password=" PLAN.md .claude/changes/ || echo "No secrets found ✅"

# Verify markdown rendering (if markdownlint installed)
npx markdownlint PLAN.md --fix 2>/dev/null || true
```
