# Layer Analysis: Dependencies & Packaging
> Generated by: WU-9 (Sequential Fallback) | Date: 2026-03-14

## Summary
- Several dependencies are flagged as problematic or unused: `@supabase/auth-helpers-nextjs` (deprecated), `zustand` listed but no store files found, `tailwindcss@^4.0.0` (major version bump), `maplibre-gl` and `cesium` large bundles.
- Docker images in docker-compose.yml use `:latest` (martin, localstack) which harms reproducibility.
- No automated dependency update tooling (dependabot/renovate) was found in the repo root.

## Findings
- Deprecated package: `@supabase/auth-helpers-nextjs@^0.15.0` — recommend migrating to `@supabase/ssr` (analysis_03_services_api.md).
- Unused dependency: `zustand` appears in package.json but no `create()` stores found in repo (analysis_01_frontend.md lines 42–49).
- Tailwind v4 upgrade may cause breaking classnames/config incompatibility (analysis_01_frontend.md lines 51–58).
- Martin image unpinned: `ghcr.io/maplibre/martin:latest` in docker-compose.yml (analysis_05_devops.md).

## Recommendations
P0 — Must fix before M17 ships
1. Migrate off deprecated `@supabase/auth-helpers-nextjs` to `@supabase/ssr` (see analysis_03_services_api.md Finding 3.2). Update imports in src/lib/supabase/server.ts, client.ts, and middleware.ts.
   - Effort: M | Risk: MED

2. Pin container images in docker-compose.yml (Martin, LocalStack, PostGIS to explicit version tags). Add image digest locking if possible.
   - Effort: S | Risk: LOW

P1 — Should fix this sprint
3. Audit and remove unused dependencies (e.g., zustand) or implement them properly. Run `npm prune && npm dedupe` and `npm ls` to find duplicates.
4. Add Dependabot or Renovate config to automatically propose version updates and security patches.
   - Files: .github/dependabot.yml or renovate.json
   - Effort: S | Risk: LOW

P2 — Nice to have
5. Introduce a lightweight dependency policy: run `npm audit --audit-level=moderate` in CI and fail on critical vulnerabilities.
6. Consider using pnpm for faster installs and more deterministic node_modules if team agrees.

## Concrete steps
- Replace `@supabase/auth-helpers-nextjs` in package.json and run `npm install @supabase/ssr` then update imports.
- Run `rg "zustand" -n` to find potential usages; if none, remove from package.json and reinstall.
- Add .github/dependabot.yml with update schedule weekly.

## Acceptance criteria
- No deprecated `@supabase/auth-helpers-nextjs` imports remain
- Docker compose uses pinned image tags and documented in CHANGELOG
- Dependabot/renovate present and creating PRs

## Rollback / Mitigation
- If replacing auth helpers breaks auth flows, revert the change and add a small compatibility shim that maps current behavior while migrating incrementally.

Sources / Evidence
- package.json dependency entries (analysis_01_frontend.md, analysis_03_services_api.md)
- docker-compose.yml lines referencing martin and localstack (analysis_05_devops.md)
